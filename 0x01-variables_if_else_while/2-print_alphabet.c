#include <stdio.h>

/**
 *main - print numbers using even
 *Return: 0 if exited properly
 */
int main(void)
{
	int ch;

	for (ch = 97; ch <= 122; ch++)
	{
		putchar (ch);
	}
	putchar (10);
	return (0);
}
