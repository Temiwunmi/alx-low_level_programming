#include <stdio.h>

/**
 * main - write a prog that prints its name, followed by new line
 * @argc: argument count
 * @argv: arguement vector
 * Return: return 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
