#include <stdio.h>

/**
 * main - Entry point
 * Return: 0 if exixed properly, non-zero otherwise
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
